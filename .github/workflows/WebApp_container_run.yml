name: WebApp_container_CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Build and run the docker container
        # run:  sh -c "docker compose up --build"
        run:  sh -c "ip a"
